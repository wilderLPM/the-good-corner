export type Ad = {
    id: number
    title: string
    description: string
    picture: string
    price: number
    location: string
    created_at: Date
}
export type Category = {
    id: number
    name: string
}