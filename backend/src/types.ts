export type Ad = {
    id: number
    title: string
    description: string
    price: number
    picture: string
    location: string
    created_at: string
    category_id: number
}

export type Category = {
    id: number
    name: string
}

